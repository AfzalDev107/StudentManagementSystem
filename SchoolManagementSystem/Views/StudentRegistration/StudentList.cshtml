@* @model IEnumerable<SchoolManagementSystem.DAL.Models.Student>

<h2>Registered Students</h2>
<table>
    <tr>
        <th>Student ID</th>
        <th>Name</th>
        <th>Email</th>
        <th>Phone</th>
        <th>Qualifications</th>
    </tr>
    @foreach (var student in Model)
    {
        <tr>
            <td>@student.StudentId</td>
            <td>@student.FirstName @student.LastName</td>
            <td>@student.Email</td>
            <td>@student.Phone</td>
            <td>
                <ul>
                    @foreach (var qual in student.Qualifications)
                    {
                        <li>@qual.CourseName - @qual.University (@qual.Year): @qual.Percentage%</li>
                    }
                </ul>
            </td>
        </tr>
    }
</table>
 *@

@model IEnumerable<SchoolManagementSystem.DAL.Models.Student>

<h2>Registered Students</h2>

<!-- Add some styling for the table -->
<style>
    table {
        width: 100%;
        border-collapse: collapse;
        margin: 20px 0;
        font-family: Arial, sans-serif;
        box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
    }

    th, td {
        padding: 10px 15px;
        text-align: left;
        border: 1px solid #ddd;
    }

    th {
        background-color: #4CAF50;
        color: white;
        font-size: 16px;
    }

    tr:nth-child(even) {
        background-color: #f2f2f2;
    }

    tr:hover {
        background-color: #ddd;
    }

    td ul {
        padding-left: 20px;
        margin: 0;
    }

        td ul li {
            list-style-type: none;
            padding: 2px 0;
        }

    /* Styling for the header */
    h2 {
        font-family: 'Arial', sans-serif;
        color: #333;
        text-align: center;
        margin-bottom: 30px;
    }
</style>

<table>
    <thead>
        <tr>
            <th>Student ID</th>
            <th>Name</th>
            <th>Email</th>
            <th>Phone</th>
            <th>Qualifications</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var student in Model)
        {
            <tr>
                <td>@student.StudentId</td>
                <td>@student.FirstName @student.LastName</td>
                <td>@student.Email</td>
                <td>@student.Phone</td>
                <td>
                    <ul>
                        @foreach (var qual in student.Qualifications)
                        {
                            <li>@qual.CourseName - @qual.University (@qual.Year): @qual.Percentage%</li>
                        }
                    </ul>
                </td>
            </tr>
        }
    </tbody>
</table>
